%nav{class: 'navbar navbar-default navbar-fixed-top', role: 'navigation' }
  %div.container-fluid
    %div.navbar-header
      %div.nav-center
      -if @muni
        =link_to "Housing #{@muni} MA",municipality_path(@muni.downcase), class: 'navbar-brand'
      -else
        =link_to "Housing MA", root_path, class: 'navbar-brand'
    -if @muni
      =render partial: 'layouts/services'
      =render partial: 'layouts/change_town'

%div.header-pad